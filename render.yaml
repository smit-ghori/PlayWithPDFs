services:
  - type: web
    name: pdf-tools
    runtime: docker
    dockerfilePath: ./Dockerfile

    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /ms-playwright

      - key: SECRET_KEY
        value: your-secret-key
