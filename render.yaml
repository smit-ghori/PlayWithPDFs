services:
  - type: web
    name: pdf-tools
    runtime: docker
    dockerfilePath: ./Dockerfile

    envVars:
      - key: SECRET_KEY
        value: your-secret-key
      - key: MAIL_USERNAME
        fromSecret: MAIL_USERNAME
      - key: MAIL_PASSWORD
        fromSecret: MAIL_PASSWORD
