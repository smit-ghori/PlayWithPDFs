services:
  - type: web
    name: pdf-tools
    env: python
    pythonVersion: "3.11.9"

    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y ghostscript pandoc

    startCommand: gunicorn main:app